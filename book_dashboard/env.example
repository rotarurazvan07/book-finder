# Copy this file to .env and fill in your values
# Used by docker-compose.yml

# Absolute or relative path to your SQLite database on the host machine
# Default: ../merge.db  (repo root)
BOOKS_DB_HOST_PATH=../merge.db

# Port to expose the frontend on the host
# Default: 3000
FRONTEND_PORT=3000
